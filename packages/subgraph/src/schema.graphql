type UserTokenProfile @entity {
  id: ID!
  sender: Bytes!
  token: Bytes!
  pending: BigInt!
  locked: BigInt!
  redeem: BigInt!
}

type TokenProfile @entity {
  id: ID!
  pendingDeposit: BigInt!
  pendingRedeem: BigInt!
}
